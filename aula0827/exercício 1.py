# Descobrindo a área de um triângulo
# b = base
# h = altura

b = float(input("Quanto é a base? "))
h = float(input("Quanto é a altura? "))

area = b*h / 2

print("Se a base é ", b, "e a altura é ", h, "então a área será", area)
